import { GrappStoreShell } from "@/components/grappstore/GrappStoreShell";

export default function GrappStoreLayout({ children }: { children: React.ReactNode }) {
  return <GrappStoreShell>{children}</GrappStoreShell>;
}
